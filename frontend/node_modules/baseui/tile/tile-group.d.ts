import * as React from 'react';
import type { TileGroupProps } from './types';
declare const TileGroup: ({ overrides, children, selected, kind, ariaLabel, disabled, onClick, }: TileGroupProps) => React.JSX.Element;
export default TileGroup;

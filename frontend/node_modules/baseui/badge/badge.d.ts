import * as React from 'react';
import type { BadgeProps } from './types';
declare const Badge: ({ children, content, color, shape, placement, hierarchy, horizontalOffset, verticalOffset, hidden, overrides, }: BadgeProps) => React.JSX.Element;
export default Badge;

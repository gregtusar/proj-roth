import { PrismaClientInput } from 'nexus-prisma/dist/types';
import { Config, DatamodelInfo, InputConfig } from './types';
export declare const DEFAULTS: Config;
export declare const DEFAULT_META_SCHEMA_DIR = "./.yoga/nexus-prisma/";
/**
 * - Compute paths relative to the root of the project
 * - Set defaults when needed
 */
export declare function normalizeConfig(config: InputConfig, projectDir: string): Config;
export declare function client(projectDir: string, input: PrismaClientInput | undefined, datamodelInfo: DatamodelInfo): PrismaClientInput;
export declare function datamodelInfo(projectDir: string, input: string | undefined): DatamodelInfo;

import * as React from 'react';
import type { StepperProps } from './types';
export declare function Stepper({ value, disabled, overrides, setValue, minValue, maxValue, }: StepperProps): React.JSX.Element;
export default Stepper;

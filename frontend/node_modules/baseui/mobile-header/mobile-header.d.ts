import * as React from 'react';
import type { MobileHeaderProps } from './types';
export declare function MobileHeader({ overrides, title, navButton, actionButtons, type, expanded, }: MobileHeaderProps): React.JSX.Element;
export default MobileHeader;

import * as React from 'react';
import type { ProgressStepsProps } from './types';
declare function ProgressSteps({ overrides, current, alwaysShowDescription, orientation, children, }: ProgressStepsProps): React.JSX.Element;
declare namespace ProgressSteps {
    var defaultProps: {
        current: number;
    };
}
export default ProgressSteps;

/// <reference types="node" />
import { ChildProcess, SpawnOptions as NodeSpawnOptions } from 'child_process';
interface SpawnOptions extends NodeSpawnOptions {
    ignoreStdio?: boolean;
}
interface SpawnPromise<T> extends Promise<T> {
    child: ChildProcess;
}
interface SpawnResult {
    pid: number;
    output: string[];
    stdout: string;
    stderr: string;
    status: number | null;
    signal: string | null;
}
export declare function spawnAsync(command: string, args?: ReadonlyArray<string>, options?: SpawnOptions): SpawnPromise<SpawnResult>;
export {};

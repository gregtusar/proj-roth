import * as React from 'react';
import type { ColumnOptions, Row } from './types';
type MeasureColumnWidthsProps = {
    columns: ColumnOptions[];
    isSelectable: boolean;
    onWidthsChange: (a: number[]) => void;
    rows: Row[];
    widths: number[];
};
export default function MeasureColumnWidths({ columns, rows, widths, isSelectable, onWidthsChange, }: MeasureColumnWidthsProps): React.JSX.Element;
export {};

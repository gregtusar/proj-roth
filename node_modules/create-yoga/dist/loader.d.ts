import { Template } from './templates';
export interface LoadOptions {
    installDependencies: boolean;
}
export declare function loadYogaStarter(template: Template, output: string, options: LoadOptions): Promise<void>;

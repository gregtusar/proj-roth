import * as React from 'react';
import type { SliderProps } from './types';
declare function Slider({ overrides, disabled, marks, onChange, onFinalChange, min, max, step, persistentThumb, valueToLabel, value: providedValue, }: SliderProps): React.JSX.Element;
export default Slider;

import * as React from 'react';
import type { SheetProps } from './types';
export declare const Sheet: ({ children, content, description, draggable, leadingAction, overrides, progressBar, title, topPosition, trailingAction, }: SheetProps) => React.JSX.Element;
export default Sheet;

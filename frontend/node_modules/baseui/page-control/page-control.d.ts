import * as React from 'react';
import type { PageControlProps } from './types';
declare const PageControl: ({ currentPage, numPages, onPageChange, kind, disabled, "aria-label": ariaLabel, overrides, }: PageControlProps) => React.JSX.Element;
export default PageControl;

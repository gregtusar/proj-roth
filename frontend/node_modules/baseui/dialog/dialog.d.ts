import React from 'react';
import type { DialogProps } from './types';
declare const Dialog: ({ artwork, buttonDock, children, handleDismiss, showDismissButton, hasOverlay, heading, isOpen, overrides, placement, numHeadingLines, size, }: DialogProps) => React.JSX.Element;
export default Dialog;

import * as React from 'react';
import type { MaskedInputProps } from './types';
declare function MaskedInput({ mask, maskChar, overrides: { Input: inputOverride, ...restOverrides }, ...restProps }: MaskedInputProps): React.JSX.Element;
declare namespace MaskedInput {
    var defaultProps: {
        maskChar: string;
    };
}
export default MaskedInput;

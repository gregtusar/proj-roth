import * as React from 'react';
import type { HintDotProps } from './types';
declare const HintDot: ({ children, color, horizontalOffset: horizontalOffsetProp, verticalOffset: verticalOffsetProp, hidden, overrides, }: HintDotProps) => React.JSX.Element;
export default HintDot;

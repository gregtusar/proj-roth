import * as React from 'react';
import type { NotificationCircleProps } from './types';
declare const NotificationCircle: ({ children, content: contentProp, color, placement, horizontalOffset, verticalOffset, hidden, overrides, }: NotificationCircleProps) => React.JSX.Element;
export default NotificationCircle;

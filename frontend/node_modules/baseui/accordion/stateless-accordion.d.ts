import * as React from 'react';
import type { StatelessAccordionProps } from './types';
declare function StatelessAccordion({ accordion, children, disabled, expanded, onChange, overrides, renderAll, }: StatelessAccordionProps): React.JSX.Element;
export default StatelessAccordion;
